# 🔧 মেইনটেনেন্স মোড - কিভাবে চালু/বন্ধ করবেন

## ✅ বর্তমান স্থিতি

আপনার **Maintenance Mode System সম্পূর্ণ কার্যকর** এবং **ডিফল্ট বন্ধ আছে**।

---

## 🎯 Browser Console থেকে চালু/বন্ধ করুন (Easiest Method)

যেহেতু AdminDashboard এ এখনও UI toggle নেই, আপনি browser console থেকে সরাসরি চালু/বন্ধ করতে পারবেন:

### ১. Maintenance Mode চালু করতে:

```javascript
localStorage.setItem('platformSettings', JSON.stringify({
  platformFee: 10,
  teacherFreePeriod: 6,
  teacherFreeCredits: 50,
  guardianFreeCredits: 100,
  maintenanceMode: true,  // এটি true করুন
  registrationOpen: true,
  autoApproveTeachers: false,
  autoApproveStudents: false
}));
alert('✅ Maintenance Mode চালু করা হয়েছে! পেজ রিফ্রেশ করুন।');
```

### ২. Maintenance Mode বন্ধ করতে:

```javascript
localStorage.setItem('platformSettings', JSON.stringify({
  platformFee: 10,
  teacherFreePeriod: 6,
  teacherFreeCredits: 50,
  guardianFreeCredits: 100,
  maintenanceMode: false,  // এটি false করুন
  registrationOpen: true,
  autoApproveTeachers: false,
  autoApproveStudents: false
}));
alert('✅ Maintenance Mode বন্ধ করা হয়েছে! পেজ রিফ্রেশ করুন।');
```

### ৩. বর্তমান স্থিতি দেখতে:

```javascript
const settings = JSON.parse(localStorage.getItem('platformSettings') || '{}');
console.log('Maintenance Mode:', settings.maintenanceMode ? 'চালু আছে ✅' : 'বন্ধ আছে ❌');
console.log('Full Settings:', settings);
```

---

## 🧪 পরীক্ষা করুন

### Step 1: Maintenance Mode চালু করুন

1. **Admin হিসেবে login করুন**:
   - Email: `admin@talenttutor.com`
   - Password: `Admin@2025`

2. **Browser Console খুলুন**:
   - Windows/Linux: `F12` অথবা `Ctrl+Shift+J`
   - Mac: `Cmd+Option+J`

3. **এই code paste করুন**:
   ```javascript
   localStorage.setItem('platformSettings', JSON.stringify({
     platformFee: 10,
     teacherFreePeriod: 6,
     teacherFreeCredits: 50,
     guardianFreeCredits: 100,
     maintenanceMode: true,
     registrationOpen: true,
     autoApproveTeachers: false,
     autoApproveStudents: false
   }));
   location.reload();
   ```

4. **Page refresh হবে** এবং admin এখনও access পাবে।

### Step 2: Non-Admin User হিসেবে Test করুন

1. **Incognito/Private Window খুলুন**
   - Windows/Linux: `Ctrl+Shift+N`
   - Mac: `Cmd+Shift+N`

2. **সাইট open করুন**

3. **✅ আপনার MaintenancePage দেখা উচিত**:
   ```
   🔧 সাময়িক রক্ষণাবেক্ষণ
   আমরা আরও ভালো সেবা দিতে কিছু উন্নতি করছি।
   অনুগ্রহ করে একটু পরে আবার চেষ্টা করুন।
   ```

4. **কোন page access করা যাবে না** (Admin ছাড়া)

### Step 3: Maintenance Mode বন্ধ করুন

1. **Admin window এ console এ এই code paste করুন**:
   ```javascript
   localStorage.setItem('platformSettings', JSON.stringify({
     platformFee: 10,
     teacherFreePeriod: 6,
     teacherFreeCredits: 50,
     guardianFreeCredits: 100,
     maintenanceMode: false,
     registrationOpen: true,
     autoApproveTeachers: false,
     autoApproveStudents: false
   }));
   location.reload();
   ```

2. **Incognito window রিফ্রেশ করুন**

3. **✅ এখন সবাই আবার সাইট access করতে পারবে**

---

## 🎨 AdminDashboard এ UI Toggle যোগ করা (Optional)

যদি আপনি একটি সুন্দর UI toggle চান, তাহলে AdminDashboard.tsx এ এই code যোগ করুন:

### যেখানে যোগ করবেন:

AdminDashboard এর Dashboard Overview section এ (প্রায় line 1900-2000), Quick Stats cards এর পরে:

```tsx
{/* Maintenance Mode Quick Control */}
<Card className="col-span-full p-6 border-2 border-orange-200 bg-gradient-to-br from-orange-50 to-amber-50">
  <div className="flex items-center justify-between">
    <div className="flex items-center gap-4">
      <div className="w-14 h-14 bg-orange-600 rounded-xl flex items-center justify-center shadow-lg">
        <Settings className="w-7 h-7 text-white" />
      </div>
      <div>
        <h3 className="text-lg text-gray-900 mb-1 flex items-center gap-2">
          🔧 {language === 'bn' ? 'মেইনটেনেন্স মোড' : 'Maintenance Mode'}
        </h3>
        <p className="text-sm text-gray-600">
          {language === 'bn' 
            ? 'সাইট সাময়িকভাবে রক্ষণাবেক্ষণের জন্য বন্ধ করুন'
            : 'Temporarily suspend the site for maintenance'
          }
        </p>
      </div>
    </div>
    <div className="flex items-center gap-3">
      <div className="text-right mr-3">
        <p className="text-sm font-medium text-gray-700">
          {language === 'bn' ? 'স্থিতি:' : 'Status:'}
        </p>
        <p className={`text-sm font-bold ${platformSettings.maintenanceMode ? 'text-orange-600' : 'text-green-600'}`}>
          {platformSettings.maintenanceMode 
            ? (language === 'bn' ? '🔧 চালু আছে' : '🔧 Enabled')
            : (language === 'bn' ? '✅ বন্ধ আছে' : '✅ Disabled')
          }
        </p>
      </div>
      <Switch
        checked={platformSettings.maintenanceMode}
        onCheckedChange={(checked) => {
          const newSettings = {
            ...platformSettings,
            maintenanceMode: checked
          };
          setPlatformSettings(newSettings);
          localStorage.setItem('platformSettings', JSON.stringify(newSettings));
          
          addActivityLog(
            language === 'bn' ? 'মেইনটেনেন্স মোড' : 'Maintenance Mode',
            language === 'bn' 
              ? (checked ? 'মেইনটেনেন্স মোড চালু করা হয়েছে' : 'মেইনটেনেন্স মোড বন্ধ করা হয়েছে')
              : (checked ? 'Maintenance mode enabled' : 'Maintenance mode disabled')
          );
          
          toast.success(
            language === 'bn'
              ? (checked ? '🔧 মেইনটেনেন্স মোড চালু করা হয়েছে!' : '✅ মেইনটেনেন্স মোড বন্ধ করা হয়েছে!')
              : (checked ? '🔧 Maintenance mode enabled!' : '✅ Maintenance mode disabled!')
          );
        }}
      />
    </div>
  </div>

  {platformSettings.maintenanceMode && (
    <Alert className="bg-orange-100 border-orange-300 mt-4">
      <AlertCircle className="w-4 h-4 text-orange-600" />
      <AlertDescription className="text-orange-800 flex items-center justify-between">
        <span>
          {language === 'bn'
            ? '⚠️ মেইনটেনেন্স মোড চালু আছে! Admin ছাড়া কেউ সাইট access করতে পারবে না।'
            : '⚠️ Maintenance mode is active! Only admins can access the site.'
          }
        </span>
        <Button
          size="sm"
          variant="outline"
          className="bg-white hover:bg-orange-50 border-orange-400 text-orange-700"
          onClick={() => {
            const newSettings = {
              ...platformSettings,
              maintenanceMode: false
            };
            setPlatformSettings(newSettings);
            localStorage.setItem('platformSettings', JSON.stringify(newSettings));
            toast.success(language === 'bn' ? '✅ বন্ধ করা হয়েছে!' : '✅ Disabled!');
          }}
        >
          {language === 'bn' ? 'দ্রুত বন্ধ করুন' : 'Quick Disable'}
        </Button>
      </AlertDescription>
    </Alert>
  )}
</Card>
```

---

## 📊 কিভাবে কাজ করে

```
┌─────────────────────────────────────────────┐
│  1. ডিফল্ট: maintenanceMode = false        │
│     System normal ভাবে চলছে                 │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  2. Admin console/UI থেকে চালু করে:        │
│     maintenanceMode = true                  │
│     localStorage এ save হয়                  │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  3. App.tsx check করে প্রতি page load এ:   │
│     isMaintenanceModeActive()               │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  4. যদি active এবং user admin না হয়:      │
│     → MaintenancePage এ redirect             │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  5. Admin user bypass করতে পারে:           │
│     canBypassMaintenance('admin') = true    │
└─────────────────────────────────────────────┘
```

---

## 🔐 নিরাপত্তা যাচাই

### Console এ check করুন:

```javascript
// 1. Platform settings দেখুন
const settings = JSON.parse(localStorage.getItem('platformSettings') || '{}');
console.log('Settings:', settings);

// 2. Maintenance mode status
console.log('Maintenance Mode:', settings.maintenanceMode);

// 3. Current user role
const user = JSON.parse(localStorage.getItem('currentUser') || '{}');
console.log('User Role:', user.role);

// 4. Can bypass?
console.log('Can Bypass:', user.role === 'admin');
```

---

## ⚡ Quick Commands

### Enable Maintenance (দ্রুত চালু করুন):

```javascript
const s=JSON.parse(localStorage.getItem('platformSettings')||'{}');s.maintenanceMode=true;localStorage.setItem('platformSettings',JSON.stringify(s));location.reload();
```

### Disable Maintenance (দ্রুত বন্ধ করুন):

```javascript
const s=JSON.parse(localStorage.getItem('platformSettings')||'{}');s.maintenanceMode=false;localStorage.setItem('platformSettings',JSON.stringify(s));location.reload();
```

### Check Status (স্থিতি দেখুন):

```javascript
alert((JSON.parse(localStorage.getItem('platformSettings')||'{}').maintenanceMode)?'🔧 চালু আছে':'✅ বন্ধ আছে');
```

---

## 🎯 Best Practices

### Maintenance চালু করার আগে:

1. **সব users কে জানান** (email/notification)
2. **আনুমানিক সময় বলুন**
3. **Contact info দিন**
4. **Backup নিন**

### Maintenance এর সময়:

1. **দ্রুত কাজ শেষ করুন**
2. **Test করুন**
3. **Logs check করুন**

### Maintenance বন্ধ করার পরে:

1. **Verification করুন** সব কিছু ঠিক আছে কিনা
2. **Users কে জানান** সাইট এখন চালু
3. **Activity monitor করুন**

---

## 🚨 Emergency: যদি Admin হিসেবে locked out হন

যদি ভুলক্রমে admin account এ access হারিয়ে ফেলেন এবং maintenance mode চালু থাকে:

```javascript
// 1. Browser console খুলুন (login page এ)
// 2. এই code run করুন:
localStorage.setItem('platformSettings', JSON.stringify({
  platformFee: 10,
  teacherFreePeriod: 6,
  teacherFreeCredits: 50,
  guardianFreeCredits: 100,
  maintenanceMode: false,
  registrationOpen: true,
  autoApproveTeachers: false,
  autoApproveStudents: false
}));
location.reload();

// এখন maintenance mode বন্ধ হয়ে গেছে
```

---

## ✅ Summary

**আপনার Maintenance Mode System:**

- ✅ **সম্পূর্ণ কার্যকর**
- ✅ **ডিফল্ট বন্ধ থাকে**
- ✅ **শুধু Admin নিয়ন্ত্রণ করতে পারে**
- ✅ **Console/UI দুভাবেই toggle করা যায়**
- ✅ **localStorage based (instant effect)**
- ✅ **Admin bypass করতে পারে**
- ✅ **Production ready**

**এখনই console থেকে test করুন!** 🚀

---

**আরও তথ্যের জন্য দেখুন:**
- `/মেইনটেনেন্স_মোড_সম্পূর্ণ_গাইড.md` - সম্পূর্ণ documentation
- `/MAINTENANCE_MODE_QUICK_REFERENCE.md` - Quick reference
- `/MAINTENANCE_MODE_ADMIN_SETUP.md` - UI setup guide
