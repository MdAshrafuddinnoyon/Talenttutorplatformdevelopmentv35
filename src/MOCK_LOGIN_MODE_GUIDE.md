# 🎭 Mock Login Mode Guide | ডেমো লগইন মোড গাইড

## ✅ সমাধান সম্পূর্ণ হয়েছে | Solution Complete

আপনার অনুরোধ অনুযায়ী, এখন **যেকোনো ইমেইল এবং পাসওয়ার্ড** দিয়ে লগইন এবং রেজিস্ট্রেশন করা যাবে, Supabase database verification ছাড়াই।

---

## 🎯 বর্তমান অবস্থা | Current Status

### ✅ কি কাজ করছে:

1. **Mock Login Mode সক্রিয়** - যেকোনো ইমেইল/পাসওয়ার্ড দিয়ে লগইন করা যাবে
2. **Mock Registration Mode সক্রিয়** - যেকোনো তথ্য দিয়ে নিবন্ধন করা যাবে
3. **সব ইউজার রোল সাপোর্ট** - শিক্ষক, অভিভাবক, ছাত্র, অ্যাডমিন, দাতা (যাকাত/উপকরণ)
4. **স্বয়ংক্রিয় ক্রেডিট** - শিক্ষক: 50, অভিভাবক: 100, অ্যাডমিন: 999
5. **LocalStorage সংরক্ষণ** - সব ইউজার ডাটা স্থানীয়ভাবে সেভ হয়
6. **ডেমো মোড ইন্ডিকেটর** - UI তে স্পষ্ট নির্দেশক দেখা যাবে

---

## 🚀 কিভাবে ব্যবহার করবেন | How to Use

### বাংলায়:

1. **লগইন পেজে যান**
2. **আপনার ইউজার টাইপ নির্বাচন করুন** (শিক্ষক/অভিভাবক/ছাত্র/দাতা/অ্যাডমিন)
3. **যেকোনো ইমেইল লিখুন** (উদাহরণ: `test@example.com`)
4. **যেকোনো পাসওয়ার্ড লিখুন** (উদাহরণ: `123456`)
5. **লগইন বাটনে ক্লিক করুন**
6. **✅ সফল! আপনি এখন লগইন হয়েছেন এবং ড্যাশবোর্ডে যাবেন**

### In English:

1. **Go to Login Page**
2. **Select your user type** (Teacher/Guardian/Student/Donor/Admin)
3. **Enter any email** (Example: `test@example.com`)
4. **Enter any password** (Example: `123456`)
5. **Click Login button**
6. **✅ Success! You are now logged in and will go to dashboard**

---

## 📝 উদাহরণ লগইন | Example Logins

আপনি এই তথ্য দিয়ে লগইন করতে পারেন:

### শিক্ষক | Teacher:
- **ইমেইল**: `teacher@test.com` বা যেকোনো ইমেইল
- **পাসওয়ার্ড**: `password` বা যেকোনো পাসওয়ার্ড
- **ক্রেডিট**: স্বয়ংক্রিয় 50 ক্রেডিট

### অভিভাবক | Guardian:
- **ইমেইল**: `guardian@test.com` বা যেকোনো ইমেইল
- **পাসওয়ার্ড**: `password` বা যেকোনো পাসওয়ার্ড
- **ক্রেডিট**: স্বয়ংক্রিয় 100 ক্রেডিট

### ছাত্র | Student:
- **ইমেইল**: `student@test.com` বা যেকোনো ইমেইল
- **পাসওয়ার্ড**: `password` বা যেকোনো পাসওয়ার্ড
- **ক্রেডিট**: 0 ক্রেডিট (সাহায্য প্রার্থী)

### অ্যাডমিন | Admin:
- **ইমেইল**: `admin@test.com` বা যেকোনো ইমেইল
- **পাসওয়ার্ড**: `password` বা যেকোনো পাসওয়ার্ড
- **ক্রেডিট**: 999 ক্রেডিট

### যাকাত দাতা | Zakat Donor:
- **ইমেইল**: `donor@test.com` বা যেকোনো ইমেইল
- **পাসওয়ার্ড**: `password` বা যেকোনো পাসওয়ার্ড
- **টাইপ**: যাকাত প্রদানকারী নির্বাচন করুন

---

## ⚙️ মোড পরিবর্তন | Mode Switching

### Mock Mode থেকে Supabase Mode এ যেতে:

**ফাইল**: `/utils/authService.ts`

```typescript
// লাইন 10 এ পরিবর্তন করুন:
const ENABLE_MOCK_MODE = false; // true থেকে false করুন
```

### Supabase Mode থেকে Mock Mode এ যেতে:

```typescript
// লাইন 10 এ পরিবর্তন করুন:
const ENABLE_MOCK_MODE = true; // false থেকে true করুন
```

---

## 🔧 প্রযুক্তিগত বিবরণ | Technical Details

### Mock Login কিভাবে কাজ করে:

1. **ইউজার ইনপুট গ্রহণ** - যেকোনো ইমেইল/পাসওয়ার্ড
2. **Mock User তৈরি** - একটি অস্থায়ী ইউজার অবজেক্ট তৈরি হয়
3. **Role অনুযায়ী Credit** - নির্বাচিত রোল অনুযায়ী ক্রেডিট বরাদ্দ
4. **LocalStorage সংরক্ষণ** - `currentUser` এবং `auth_token` সেভ হয়
5. **Mock Token তৈরি** - `mock_token_<timestamp>_<random>` ফরম্যাটে

### ফাইল পরিবর্তন সমূহ:

#### 1. `/utils/authService.ts`
- ✅ `ENABLE_MOCK_MODE` কনফিগ যোগ করা হয়েছে
- ✅ `mockLogin()` ফাংশন যোগ করা হয়েছে
- ✅ `mockRegister()` ফাংশন যোগ করা হয়েছে
- ✅ `login()` এ mock mode চেক যোগ করা হয়েছে
- ✅ `register()` এ mock mode চেক যোগ করা হয়েছে

#### 2. `/components/UnifiedAuthDialog.tsx`
- ✅ Mock Mode indicator badge যোগ করা হয়েছে
- ✅ Helper text যোগ করা হয়েছে (লগইন এবং রেজিস্ট্রেশন ফর্মে)
- ✅ `selectedRole` প্যারামিটার `login()` এ পাঠানো হচ্ছে

---

## 🎨 UI পরিবর্তন | UI Changes

### নতুন উপাদান:

1. **🎭 ডেমো মোড Badge** - Dialog এর উপরের ডান কোণায়
2. **🎯 Helper Text Box** - লগইন এবং রেজিস্ট্রেশন ফর্মে সবুজ বক্স
3. **Success Toast** - "Mock login successful (Development Mode)"

---

## ⚠️ গুরুত্বপূর্ণ নোট | Important Notes

### বাংলায়:

1. **ডেটা স্থায়ী নয়** - Browser refresh করলে বা localStorage clear করলে ডেটা মুছে যাবে
2. **প্রোডাকশনে নিষিদ্ধ** - Live site এ `ENABLE_MOCK_MODE = false` রাখুন
3. **Testing এর জন্য আদর্শ** - Development এবং UI testing এর জন্য উপযুক্ত
4. **Backend যোগাযোগ নেই** - কোনো API call হয় না mock mode এ

### In English:

1. **Data is Not Persistent** - Data will be lost on browser refresh or localStorage clear
2. **Not for Production** - Keep `ENABLE_MOCK_MODE = false` on live site
3. **Perfect for Testing** - Ideal for development and UI testing
4. **No Backend Communication** - No API calls are made in mock mode

---

## 🐛 সমস্যা সমাধান | Troubleshooting

### সমস্যা: লগইন করার পর ড্যাশবোর্ডে যাচ্ছে না

**সমাধান**:
```javascript
// Browser Console এ চেক করুন:
localStorage.getItem('currentUser')
localStorage.getItem('auth_token')

// যদি null হয়, আবার লগইন করুন
```

### সমস্যা: "Mock Mode" দেখা যাচ্ছে না

**সমাধান**:
1. `/utils/authService.ts` চেক করুন
2. `ENABLE_MOCK_MODE = true` আছে কিনা নিশ্চিত করুন
3. Browser cache clear করুন
4. Page refresh করুন

### সমস্যা: Supabase Mode এ ফিরে যেতে চাই

**সমাধান**:
```typescript
// /utils/authService.ts - Line 10
const ENABLE_MOCK_MODE = false; // Change to false
```

---

## 📊 কনসোল লগ | Console Logs

Mock mode এ নিম্নলিখিত logs দেখতে পাবেন:

```
🎭 Using Mock Authentication Mode
🔐 Mock Login (Development Mode): test@example.com
✅ Mock login successful: {id: "mock_...", name: "Test", ...}
```

Supabase mode এ:
```
🔐 Login attempt with Supabase Auth: test@example.com
✅ Supabase Auth login successful: abc123...
```

---

## 🎉 সংক্ষেপে | Summary

- ✅ **Mock Mode সক্রিয়** - কোনো database প্রয়োজন নেই
- ✅ **যেকোনো ইমেইল/পাসওয়ার্ড** - তাৎক্ষণিক লগইন
- ✅ **সব রোল সাপোর্ট** - 5 ধরনের ইউজার
- ✅ **স্বয়ংক্রিয় ক্রেডিট** - রোল অনুযায়ী
- ✅ **সহজ পরীক্ষা** - UI এবং features test করুন
- ✅ **এক ক্লিকে সুইচ** - Mock ↔ Supabase

---

## 📞 সাপোর্ট | Support

যদি কোনো সমস্যা হয়, নিশ্চিত করুন:

1. ✅ `/utils/authService.ts` এ `ENABLE_MOCK_MODE = true`
2. ✅ Browser Console এ কোনো error নেই
3. ✅ LocalStorage enabled আছে
4. ✅ JavaScript enabled আছে

---

**✨ এখন আপনি সফলভাবে Mock Mode ব্যবহার করে লগইন করতে পারবেন!**

**✨ You can now successfully login using Mock Mode!**
