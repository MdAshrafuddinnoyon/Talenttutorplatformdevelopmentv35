# 🔧 সমস্যা সমাধান গাইড - Talent Tutor

## সবচেয়ে সাধারণ সমস্যা এবং সমাধান

---

## ❌ সমস্যা ১: "npm command not found"

### কারণ:
Node.js ইনস্টল হয়নি বা PATH এ যুক্ত নেই

### সমাধান:

**Windows:**
1. https://nodejs.org থেকে LTS version ডাউনলোড করুন
2. Installer রান করুন (সব চেকবক্স চেক রাখুন)
3. কম্পিউটার restart করুন
4. নতুন Command Prompt/PowerShell খুলুন
5. Test করুন:
   ```cmd
   node -v
   npm -v
   ```

**Mac:**
```bash
# Homebrew দিয়ে:
brew install node

# অথবা nodejs.org থেকে installer ডাউনলোড করুন
```

**Linux (Ubuntu/Debian):**
```bash
# Official repository থেকে:
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# Verify:
node -v
npm -v
```

---

## ❌ সমস্যা ২: "npm install" কাজ করছে না

### সম্ভাব্য কারণ:
- Network timeout
- Corrupted cache
- Permission issues

### সমাধান:

**ধাপ ১: Cache clean করুন**
```bash
npm cache clean --force
```

**ধাপ ২: node_modules মুছে ফেলুন**
```bash
# Windows (PowerShell):
Remove-Item -Recurse -Force node_modules
Remove-Item package-lock.json

# Mac/Linux:
rm -rf node_modules package-lock.json

# Windows (CMD):
rmdir /s /q node_modules
del package-lock.json
```

**ধাপ ৩: আবার install করুন**
```bash
npm install
```

**ধাপ ৪: যদি এখনও কাজ না করে**
```bash
# Alternative registry ব্যবহার করুন:
npm config set registry https://registry.npmjs.org/
npm install

# অথবা yarn ব্যবহার করুন:
npm install -g yarn
yarn install
```

---

## ❌ সমস্যা ৩: "Cannot find module" Error

### কারণ:
Dependencies properly install হয়নি

### সমাধান:

```bash
# Option 1: Specific package install করুন
npm install [missing-package-name]

# Option 2: সব dependencies reinstall করুন
rm -rf node_modules package-lock.json
npm install

# Option 3: Check package.json
# নিশ্চিত করুন package.json ঠিক আছে এবং corrupted নয়
```

---

## ❌ সমস্যা ৪: Port already in use (EADDRINUSE)

### কারণ:
5173 port অন্য process ব্যবহার করছে

### সমাধান:

**Windows:**
```cmd
# Port ব্যবহার করছে এমন process খুঁজুন:
netstat -ano | findstr :5173

# Process ID পেয়ে task kill করুন:
taskkill /PID <process_id> /F

# অথবা Vite automatically অন্য port ব্যবহার করবে
```

**Mac/Linux:**
```bash
# Port ব্যবহার করছে এমন process খুঁজুন:
lsof -i :5173

# Process kill করুন:
kill -9 <PID>

# অথবা অন্য port ব্যবহার করুন:
npm run dev -- --port 3000
```

---

## ❌ সমস্যা ৫: Blank/White Page দেখাচ্ছে

### কারণ:
- JavaScript errors
- Routing issues
- Build problems

### সমাধান:

**ধাপ ১: Browser Console check করুন**
1. F12 চাপুন (Developer Tools)
2. Console tab এ যান
3. Red error messages দেখুন

**ধাপ ২: Common fixes**
```bash
# Development server restart করুন:
Ctrl+C (server stop করুন)
npm run dev

# Cache clear করুন:
Ctrl+Shift+R (Hard reload)

# Build আবার করুন:
npm run build
npm run preview
```

**ধাপ ৩: Base path check করুন**

যদি subdirectory তে deploy করেন, `vite.config.ts` check করুন:

```typescript
export default defineConfig({
  base: '/',  // Root এ deploy করলে '/'
  // base: '/app/', // Subdirectory তে deploy করলে '/app/'
});
```

---

## ❌ সমস্যা ৬: CSS/Styles লোড হচ্ছে না

### কারণ:
- Tailwind config issue
- Build not updated
- Cache problem

### সমাধান:

```bash
# 1. Rebuild করুন:
npm run build

# 2. Development mode এ check করুন:
npm run dev

# 3. Tailwind config verify করুন:
# tailwind.config.js file আছে কিনা check করুন

# 4. globals.css import হয়েছে কিনা check করুন:
# main.tsx অথবা App.tsx এ:
# import './styles/globals.css'
```

---

## ❌ সমস্যা ৭: Images লোড হচ্ছে না

### কারণ:
- Wrong path
- Build not including images
- Server configuration

### সমাধান:

**Development:**
```typescript
// ✅ সঠিক পথ (public folder এ রাখলে):
<img src="/images/logo.png" alt="Logo" />

// ❌ ভুল পথ:
<img src="images/logo.png" alt="Logo" />
<img src="./images/logo.png" alt="Logo" />
```

**Production:**
```bash
# Images কপি হয়েছে কিনা check করুন:
# dist/images/ ফোল্ডারে images আছে কিনা দেখুন

# যদি না থাকে, public ফোল্ডারে রাখুন:
public/
  └── images/
      └── logo.png
```

---

## ❌ সমস্যা ৮: লাইভ সার্ভারে Routes কাজ করছে না (404)

### কারণ:
Server SPA routing configured নেই

### সমাধান:

**cPanel / Apache:**

`public_html/.htaccess` ফাইল তৈরি করুন:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

**Nginx:**

```nginx
location / {
  try_files $uri $uri/ /index.html;
}
```

**Vercel/Netlify:**
- Automatically configured! কোন কাজ করতে হবে না।

---

## ❌ সমস্যা ৯: Environment Variables কাজ করছে না

### কারণ:
- `VITE_` prefix নেই
- `.env` file সঠিক জায়গায় নেই
- Server restart করা হয়নি

### সমাধান:

**সঠিক format:**
```env
# ✅ সঠিক - VITE_ prefix সহ:
VITE_GOOGLE_MAPS_API_KEY=abc123
VITE_SUPABASE_URL=https://xyz.supabase.co

# ❌ ভুল - prefix ছাড়া:
GOOGLE_MAPS_API_KEY=abc123
SUPABASE_URL=https://xyz.supabase.co
```

**File location:**
```
talent-tutor/
├── .env          # ✅ এখানে থাকতে হবে (root এ)
├── package.json
├── vite.config.ts
└── src/
```

**ব্যবহার করার নিয়ম:**
```typescript
// Code এ:
const apiKey = import.meta.env.VITE_GOOGLE_MAPS_API_KEY;

// console.log দিয়ে test করুন:
console.log('API Key:', import.meta.env.VITE_GOOGLE_MAPS_API_KEY);
```

**গুরুত্বপূর্ণ:**
- `.env` file পরিবর্তন করলে server restart করুন!
```bash
Ctrl+C
npm run dev
```

---

## ❌ সমস্যা ১০: TypeScript Errors

### সমাধান:

```bash
# 1. Type check করুন:
npx tsc --noEmit

# 2. যদি অনেক errors থাকে, একটা একটা করে fix করুন

# 3. Common fixes:
# - Missing types: npm install @types/[package-name]
# - Wrong imports: Check import paths
# - Any type: Replace with proper types

# 4. Temporary fix (NOT recommended):
# tsconfig.json এ যোগ করুন:
{
  "compilerOptions": {
    "skipLibCheck": true,
    "noEmit": true
  }
}
```

---

## ❌ সমস্যা ১১: Build Fails

### সমাধান:

```bash
# 1. Error message পড়ুন carefully
npm run build

# 2. Common issues:
# - TypeScript errors → Fix type issues
# - Missing dependencies → npm install
# - Import errors → Check paths
# - Memory issues → Increase Node memory

# 3. Memory increase (যদি large project):
# package.json এ:
"scripts": {
  "build": "NODE_OPTIONS='--max-old-space-size=4096' vite build"
}

# Windows PowerShell:
$env:NODE_OPTIONS="--max-old-space-size=4096"
npm run build
```

---

## ❌ সমস্যা ১২: Slow Performance

### কারণ:
- Too many re-renders
- Large bundle size
- Not optimized images

### সমাধান:

**Check bundle size:**
```bash
npm run build
# Check dist/assets/ file sizes
# Main JS should be < 1MB
```

**Optimize images:**
```bash
# Use WebP format
# Compress images
# Lazy load images
```

**Code splitting:**
```typescript
// Use React.lazy for route-based splitting:
const HomePage = React.lazy(() => import('./pages/HomePage'));
```

---

## 🔍 Debugging Tips

### ১. Console Logging
```typescript
console.log('Debug:', variable);
console.error('Error:', error);
console.table(arrayData);
```

### ২. React DevTools
- Install React Developer Tools (Chrome/Firefox extension)
- Inspect component props and state
- Check component hierarchy

### ৩. Network Tab
- F12 → Network tab
- Check which files are loading
- Look for 404 errors
- Check response times

### ৪. Lighthouse Audit
- F12 → Lighthouse tab
- Run audit
- Check Performance, Accessibility, SEO scores

---

## 📞 এখনও সমস্যা হচ্ছে?

### Quick Checklist:
- [ ] Node.js properly installed? (`node -v`)
- [ ] Dependencies installed? (`npm install`)
- [ ] In correct directory? (`ls` or `dir`)
- [ ] package.json exists?
- [ ] .env configured (if needed)?
- [ ] Browser console checked?
- [ ] Server restarted?
- [ ] Cache cleared?

### সাহায্যের জন্য:
1. **Error message** copy করুন
2. **Google** এ search করুন
3. **Stack Overflow** check করুন
4. **GitHub Issues** open করুন
5. **Community** তে প্রশ্ন করুন

### Debug Information Collect করুন:
```bash
# System info:
node -v
npm -v
npm list

# Error logs:
npm run dev > debug.log 2>&1
# debug.log file share করুন
```

---

## ✅ সফলভাবে চালু হলে

আপনার terminal এ দেখবেন:
```
  VITE v5.4.8  ready in 1234 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.1.100:5173/
  ➜  press h + enter to show help
```

Browser এ গেলে দেখবেন:
- ✅ সুন্দর homepage
- ✅ সব design সঠিক
- ✅ Navigation কাজ করছে
- ✅ কোন error নেই (F12 console check)

**অভিনন্দন! আপনার Talent Tutor application চলছে!** 🎉

---

## 📚 আরও তথ্যের জন্য

- **Setup Guide:** [`SETUP_LOCAL.md`](/SETUP_LOCAL.md)
- **Deployment Guide:** [`লোকাল_এবং_লাইভ_সার্ভার_ডিপ্লয়_সম্পূর্ণ_গাইড.md`](/লোকাল_এবং_লাইভ_সার্ভার_ডিপ্লয়_সম্পূর্ণ_গাইড.md)
- **Quick Start:** [`শুরু_করুন_এখানে.md`](/শুরু_করুন_এখানে.md)
