# 🚀 Talent Tutor - লোকাল এবং লাইভ সার্ভার ডিপ্লয়মেন্ট সম্পূর্ণ গাইড

## ⚠️ গুরুত্বপূর্ণ তথ্য

এই প্রজেক্টটি একটি **Modern React + Vite Application**। এটি সরাসরি HTML ফাইল হিসেবে কাজ করবে না। 

### কেন সরাসরি চলবে না?
- ❌ এটি শুধু HTML/CSS/JS নয়
- ❌ PHP সার্ভারে সরাসরি আপলোড করে চলবে না
- ❌ `index.html` ফাইল ডাবল ক্লিক করে চলবে না
- ✅ এটি চালাতে **Build Process** প্রয়োজন
- ✅ Modern **SPA (Single Page Application)** architecture

---

## 📋 Part 1: লোকাল কম্পিউটারে চালানো

### ধাপ ১: প্রয়োজনীয় সফটওয়্যার ইনস্টল

#### Node.js ইনস্টল করুন (সবচেয়ে গুরুত্বপূর্ণ!)

1. **Download করুন**: [https://nodejs.org](https://nodejs.org)
2. **LTS Version** (20.x বা তার বেশি) ডাউনলোড করুন
3. ইনস্টলার রান করে ডিফল্ট সেটিংসে ইনস্টল করুন
4. **Verify করুন** - টার্মিনালে লিখুন:

```bash
node -v
# আউটপুট হবে: v20.12.1 (বা তার কাছাকাছি)

npm -v
# আউটপুট হবে: 10.x.x (বা তার কাছাকাছি)
```

যদি ভার্সন দেখায়, তাহলে সফলভাবে ইনস্টল হয়েছে! ✅

---

### ধাপ ২: প্রজেক্ট ডাউনলোড এবং ওপেন করুন

```bash
# 1. প্রজেক্ট ফোল্ডার আনজিপ করুন
# 2. Command Prompt বা PowerShell ওপেন করুন
# 3. প্রজেক্ট ফোল্ডারে যান

cd C:\Users\YourName\Desktop\talent-tutor
# (আপনার প্রকৃত পাথ দিন)
```

**অথবা VS Code ব্যবহার করুন:**
1. VS Code ওপেন করুন
2. `File → Open Folder`
3. প্রজেক্ট ফোল্ডার সিলেক্ট করুন
4. `Terminal → New Terminal` (Ctrl + `)

---

### ধাপ ৩: Dependencies ইনস্টল করুন

```bash
npm install
```

⏱️ **এটি ৩-১০ মিনিট সময় নিতে পারে।** ইন্টারনেট থেকে সব প্যাকেজ ডাউনলোড হচ্ছে।

**সম্ভাব্য সমস্যা এবং সমাধান:**

#### ❌ Error: "npm not found"
**সমাধান:** Node.js সঠিকভাবে ইনস্টল হয়নি। আবার ইনস্টল করুন এবং কম্পিউটার রিস্টার্ট দিন।

#### ❌ Error: Permission denied
**Windows:**
```bash
# PowerShell Admin হিসেবে চালান
```

**Mac/Linux:**
```bash
sudo npm install
```

#### ❌ Error: Network timeout
```bash
# NPM registry পরিবর্তন করুন
npm config set registry https://registry.npmjs.org/
npm install
```

---

### ধাপ ৪: Development Server চালু করুন

```bash
npm run dev
```

✅ **সফল হলে দেখবেন:**
```
  VITE v5.4.8  ready in 1234 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.1.100:5173/
  ➜  press h + enter to show help
```

🌐 **ব্রাউজারে খুলুন:** http://localhost:5173

**এখন আপনার অ্যাপ্লিকেশন চলছে!** 🎉

---

### ধাপ ৫: Environment Variables সেটআপ (Optional)

```bash
# .env.example ফাইলটি কপি করুন
cp .env.example .env

# অথবা Windows এ:
copy .env.example .env
```

`.env` ফাইলে আপনার API Keys যোগ করুন:

```env
# Google Maps (Optional - ম্যাপ ফিচার এর জন্য)
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Supabase (যদি ব্যবহার করতে চান)
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_key
```

**Note:** এই keys ছাড়াই অ্যাপ চলবে, কিন্তু কিছু ফিচার (Maps, Database) কাজ করবে না।

---

## 🌍 Part 2: লাইভ সার্ভারে ডিপ্লয়মেন্ট

### Build তৈরি করা

লাইভ সার্ভারে আপলোড করার আগে **Production Build** তৈরি করতে হবে:

```bash
npm run build
```

✅ **সফল হলে:**
- `dist` ফোল্ডার তৈরি হবে
- এর ভিতরে optimized HTML, CSS, JS ফাইল থাকবে

**Build এর আউটপুট:**
```
dist/
├── index.html
├── assets/
│   ├── index-[hash].js
│   ├── index-[hash].css
│   └── [অন্যান্য ফাইল]
└── [static files]
```

---

## 🚀 Deployment Options

### Option 1: Vercel (সবচেয়ে সহজ - RECOMMENDED ✅)

#### কেন Vercel?
- ✅ **সম্পূর্ণ বিনামূল্যে**
- ✅ **Auto-deployment** (Git push করলেই deploy হয়)
- ✅ **CDN + SSL সহ** দ্রুত গতি
- ✅ **Vite/React এর জন্য perfect**

#### ধাপ:

1. **Vercel Account তৈরি করুন**
   - [https://vercel.com](https://vercel.com) এ যান
   - GitHub/GitLab দিয়ে Sign Up করুন

2. **Git Repository তৈরি করুন**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   
   # GitHub এ নতুন repository তৈরি করুন, তারপর:
   git remote add origin https://github.com/username/talent-tutor.git
   git push -u origin main
   ```

3. **Vercel এ Import করুন**
   - Vercel Dashboard → "Add New Project"
   - GitHub repository সিলেক্ট করুন
   - **Framework Preset:** Vite
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
   - "Deploy" ক্লিক করুন

4. **✅ সম্পন্ন!**
   - আপনার সাইট live: `https://talent-tutor.vercel.app`
   - প্রতিবার Git push করলে auto-deploy হবে

---

### Option 2: Netlify (খুবই জনপ্রিয়)

1. **Netlify Account তৈরি করুন**: [https://netlify.com](https://netlify.com)

2. **Drag & Drop Deployment:**
   ```bash
   npm run build
   ```
   - Netlify Dashboard → "Add new site" → "Deploy manually"
   - `dist` ফোল্ডার drag & drop করুন

3. **অথবা Git Integration:**
   - GitHub repository connect করুন
   - Build settings:
     - **Build command:** `npm run build`
     - **Publish directory:** `dist`

---

### Option 3: cPanel / Traditional Hosting

⚠️ **Important:** cPanel এ deploy করা কিছুটা জটিল, কারণ SPA routing এর জন্য বিশেষ configuration প্রয়োজন।

#### ধাপ:

1. **Build তৈরি করুন:**
   ```bash
   npm run build
   ```

2. **Upload করুন:**
   - FileZilla বা cPanel File Manager ব্যবহার করুন
   - `dist` ফোল্ডারের **সব কন্টেন্ট** `public_html` এ আপলোড করুন
   - (শুধু dist ফোল্ডার নয়, এর **ভিতরের ফাইল** গুলো)

3. **`.htaccess` ফাইল তৈরি করুন:**

`public_html/.htaccess` ফাইল তৈরি করুন এবং এই কোড যোগ করুন:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  
  # Handle React Router
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-l
  RewriteRule . /index.html [L]
</IfModule>

# Security Headers
<IfModule mod_headers.c>
  Header set X-Content-Type-Options "nosniff"
  Header set X-Frame-Options "SAMEORIGIN"
  Header set X-XSS-Protection "1; mode=block"
</IfModule>

# Compression
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css text/javascript application/javascript application/json
</IfModule>

# Cache Control
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
</IfModule>
```

4. **Verify করুন:**
   - আপনার ডোমেইনে যান: `https://yourdomain.com`
   - সব page (About, Contact, etc.) কাজ করছে কিনা চেক করুন

---

### Option 4: GitHub Pages (বিনামূল্যে)

1. **Install `gh-pages` package:**
   ```bash
   npm install --save-dev gh-pages
   ```

2. **`package.json` এ যোগ করুন:**
   ```json
   {
     "homepage": "https://username.github.io/talent-tutor",
     "scripts": {
       "predeploy": "npm run build",
       "deploy": "gh-pages -d dist"
     }
   }
   ```

3. **`vite.config.ts` update করুন:**
   ```typescript
   export default defineConfig({
     base: '/talent-tutor/',  // repository name
     // ... rest of config
   });
   ```

4. **Deploy করুন:**
   ```bash
   npm run deploy
   ```

---

## 🔍 সমস্যা সমাধান (Troubleshooting)

### সমস্যা ১: "Blank Page" দেখাচ্ছে

**কারণ:**
- Build করা হয়নি
- Base path ভুল configured

**সমাধান:**
```bash
# 1. Build আবার করুন
npm run build

# 2. Console check করুন (F12)
# যদি 404 errors দেখেন, base path ঠিক করুন

# 3. vite.config.ts check করুন
# subdirectory তে deploy করলে base path set করুন
```

---

### সমস্যা ২: Routes কাজ করছে না (404 Error)

**কারণ:** Server-side routing configured নেই

**সমাধান (cPanel):**
- `.htaccess` ফাইল সঠিকভাবে আপলোড করুন (উপরে দেওয়া)

**সমাধান (Nginx):**
```nginx
location / {
  try_files $uri $uri/ /index.html;
}
```

---

### সমস্যা ৩: CSS/Images লোড হচ্ছে না

**কারণ:** Incorrect base path

**সমাধান:**

যদি subdirectory তে host করেন (যেমন: `yourdomain.com/app`):

`vite.config.ts`:
```typescript
export default defineConfig({
  base: '/app/',  // subdirectory name
  // ...
});
```

---

### সমস্যা ৪: "npm install" ব্যর্থ হচ্ছে

**সমাধান:**
```bash
# 1. Cache clear করুন
npm cache clean --force

# 2. node_modules মুছে ফেলুন
rm -rf node_modules package-lock.json
# Windows এ: rmdir /s node_modules এবং del package-lock.json

# 3. আবার install করুন
npm install
```

---

### সমস্যা ৫: "Module not found" Error

**সমাধান:**
```bash
# সবচেয়ে common সমস্যা - dependency install হয়নি
npm install

# নির্দিষ্ট package missing হলে:
npm install [package-name]
```

---

## 📊 Deployment Comparison

| Feature | Vercel | Netlify | cPanel | GitHub Pages |
|---------|--------|---------|--------|--------------|
| **Setup Ease** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐ |
| **Free Tier** | ✅ Excellent | ✅ Good | ❌ Paid | ✅ Limited |
| **Auto Deploy** | ✅ | ✅ | ❌ | ⚠️ Manual |
| **Custom Domain** | ✅ Free | ✅ Free | ✅ | ✅ |
| **SSL Certificate** | ✅ Auto | ✅ Auto | ⚠️ Let's Encrypt | ✅ Auto |
| **Build Time** | Fast | Fast | N/A | Slow |
| **Best For** | Production | Production | Traditional | Demo |

---

## ✅ Recommended Workflow

### Development (আপনার কম্পিউটারে):
```bash
npm run dev
# Edit code → See changes instantly
```

### Testing Production Build (Deploy এর আগে):
```bash
npm run build
npm run preview
# http://localhost:4173 এ test করুন
```

### Deploy to Production:
```bash
# Option A: Vercel/Netlify (Auto)
git push

# Option B: Manual
npm run build
# dist ফোল্ডার আপলোড করুন
```

---

## 🎯 Quick Checklist

Deploy করার আগে চেক করুন:

- [ ] ✅ Node.js installed (v18+)
- [ ] ✅ `npm install` সফল
- [ ] ✅ `npm run dev` locally চলছে
- [ ] ✅ `npm run build` error ছাড়া complete হয়
- [ ] ✅ `.env` ফাইল configured (যদি প্রয়োজন হয়)
- [ ] ✅ `dist` ফোল্ডার তৈরি হয়েছে
- [ ] ✅ `.htaccess` ready (cPanel এর জন্য)
- [ ] ✅ Browser console এ error নেই

---

## 📞 সাহায্য প্রয়োজন?

**Common Issues:**
1. **লোকালে চলছে কিন্তু লাইভে না:** Base path এবং routing check করুন
2. **Build হচ্ছে না:** Dependencies এবং TypeScript errors চেক করুন
3. **Slow loading:** Compression এবং caching enable করুন

**Debug করার জন্য:**
```bash
# Detailed build output
npm run build -- --debug

# Check for errors
npm run build 2>&1 | tee build.log
```

---

## 🎉 সারসংক্ষেপ

### লোকালে চালাতে:
```bash
npm install
npm run dev
```

### Deploy করতে:
```bash
npm run build
# তারপর dist ফোল্ডার আপলোড করুন
```

**সবচেয়ে সহজ পথ:** Vercel বা Netlify ব্যবহার করুন - GitHub connect করলে automatically deploy হবে! 🚀

---

**আরও তথ্যের জন্য দেখুন:**
- `/SETUP_LOCAL.md` - বিস্তারিত লোকাল সেটআপ
- `/README.md` - প্রজেক্ট overview
- `/DOCUMENTATION_INDEX.md` - সম্পূর্ণ ডকুমেন্টেশন

**সফল ডিপ্লয়মেন্ট কামনা করছি!** 🎊
